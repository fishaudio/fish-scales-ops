# Layer performance — sm_100 / sm_103 (NVIDIA B300)

Whole MLP / MoE block per shape family as one CUDA-graph replay. Block
contents, FLOPs and the `model ms` convention: [`README.md`](README.md);
protocol, clock note and M grids: [`../README.md`](../README.md). Tables are
generated from `tests/baselines/` jsonl — do not hand-edit rows.

## Provenance and status (2026-09-22)

| section | fso rows | comparator rows | status |
|---|---|---|---|
| Family A dense MLP block | `tests/baselines/gemm_sm100_qwen3_4b_mlp_fwd.jsonl` (2026-09-22 14:16:47–14:18:36 UTC, pass 1 of two) | same jsonl, same pass: torch BF16, cuBLAS `scaled_mm` MXFP8 with reference quantize, cuBLAS `scaled_mm` MXFP8 with `torch.compile`d quantize | **accepted baseline** |
| Family B routed MoE layer | `tests/baselines/perf_moe_qwen3_30a3_b300.jsonl` (14:23:49–14:25:31 UTC, pass 1 of two) | `tests/baselines/ref_moe_qwen3_30a3_b300.jsonl` (15:24:03–15:27:26 UTC, same card, same session): torch `scaled_grouped_mm` MXFP8 / torch `_grouped_mm` BF16 | **accepted baseline** |
| Family C routed MoE layer | `tests/baselines/perf_moe_qwen3_35a3_b300.jsonl` (14:30:35–14:32:18 UTC, pass 1 of two) | `tests/baselines/ref_moe_qwen3_35a3_b300.jsonl` (15:30:52–15:34:10 UTC) | **accepted baseline** |
| Family C routed + shared expert block | `tests/baselines/perf_moe_qwen3_35a3_shared_b300.jsonl` (14:36:25–14:37:19 UTC, pass 1 of two) | `tests/baselines/ref_moe_qwen3_35a3_shared_b300.jsonl` (15:37:28–15:40:54 UTC) — on this device the comparators were measured for the shared block too, so Family C carries four comparator columns instead of two | **accepted baseline** |

All runs: the B300 SXM6 AC at PCI `E6:00.0` of pod `fish-dev-5f7c8f8bbf-hlvt4`,
**clocks unlocked** (no lock is available on this pod), every cell preceded by
`FSO_BENCH_WARM_MS=300` and taken under a `flock` that keeps one process on the
device. Driver 595.84, torch 2.11.0+cu130, CUDA 13.0, CUTLASS 4.4.2 for the
ahead-of-time kernels and `nvidia-cutlass-dsl` **4.8.0** in the virtual
environment for the CuTe-DSL rows, built `ARCH=10.0f`; commit `5c594cb` plus
the uncommitted MXFP8 change set, squashed as **this commit**. The dense decode
rows of Family A require the `sm100` extra (`nvidia-cutlass-dsl` 4.5.0 or
newer) and are **inert on 4.4.2**: below that floor the router behaves exactly
as it did before the row existed, so a deployment pinned to 4.4.2 reads the
previous run's dense decode numbers rather than these. The full environment
table, the mechanisms this run added (in the MoE layer a fused SwiGLU epilogue
on the decode route's FC1 as well as the pointer-array one, and an
EVT-partitioned direct store with the re-derived short-mainloop rule that uses
it; on the dense path a CuTe-DSL decode row in front of the three tiers for
M ≤ 32; and the packed active-expert list now asked for only where a route
reads it), the unlocked-device caveats
(about ±3 % at M ≥ 4096, the wider scatter of the M = 8192 MLP-block cells,
clock droop at M = 8192, the ~2.05 µs replay tick) and the L2 caveat are in
[`../gemm/sm100.md`](../gemm/sm100.md); they apply to every row here.

Every comparator row on this page was measured on the same card, in the same
session and on the same tree as the fso rows it sits next to, with the same
warm-up and the same graph-replay protocol. Family A's two cuBLAS columns are
cells of the same bench pass as its fso columns and share that pass's inputs.
The MoE comparators are separate passes, run at the end of the same session
under the same GPU lock, driven by the same routing generator and seed as the
fso passes, so both sides see the same expert assignment at every M. To bound
how much those comparator columns move on their own, all three torch files were
measured a second time in the same session: over the eighty-four cells, the
median pass-to-pass difference is under half a per cent.

The tables on this page have been measured on three different cards of the
same pod — `86:00.0` on 2026-09-15, `06:00.0` on 2026-09-17 and `E6:00.0` for
the 2026-09-22 runs — and each card change forced a wholesale replacement
rather than a merge, because rows from two cards are not comparable under this
protocol. How much the card alone was worth was measured when it changed: the
comparator columns of this page, which contain no fso kernel and cannot move
with a library change, read about 1.8 % faster on `E6:00.0` than the same
columns of the 2026-09-17 run. This run is the second one on `E6:00.0`, so it
replaces its predecessor for a code reason rather than a card reason, and the
comparator columns are the check on that: they are reported next to the
published ones in the run's `tables/TABLES_comparators.txt`.

Comparators (allowed only in this directory, [`../../README.md`](../../README.md) rule 2):

| column | what it is | version | caveat |
|---|---|---|---|
| cuBLAS scaled_mm MXFP8 + reference quantize | the same MLP closure with `torch.nn.functional.scaled_mm` (cuBLASLt MXFP8) and PyTorch's reference `to_mxfp` + `to_blocked` activation quantize inside the graph | torch 2.11.0+cu130 | the reference quantize is ~10 small ops per call; this is the unfused baseline |
| cuBLAS scaled_mm MXFP8 + torch.compile quantize | same, with the quantize chain wrapped in `torch.compile` so Inductor fuses it | torch 2.11.0+cu130 | the fair cuBLAS number |
| torch scaled_grouped_mm MXFP8 | `torch.nn.functional.scaled_grouped_mm` with MXFP8 1×32 blocked scales, driving the same six-stage MoE block (routing, gather-quant, gate_up, silu-quant, down, combine) | torch 2.11.0+cu130 | expert-sorted contiguous rows with an `offs` alignment the layout has to satisfy; the quantize stages are Inductor-generated inside the captured graph, so an Inductor version change moves these rows |
| torch _grouped_mm BF16 | `torch._grouped_mm` with BF16 weights, same block | torch 2.11.0+cu130 | a speed reference, not a competitor: it skips both quantize steps and reads two bytes per weight element instead of one |

Two structural notes on the MoE comparators. First, neither sglang nor
deep_gemm is installed on this pod, so the comparator columns here are not the
ones `sm90.md` and `sm120.md` carry; a column of this file is not comparable to
a column of those. Second, the torch rows and the fso rows share the routing
generator and its seed, so they see the same expert assignment, but the torch
layer computes exactly `M · 8` rows of GEMM while the masked-slab layout
computes `E · m_cap` rows and discards the padding — the two are comparable as
time per layer, not as arithmetic done per layer. The routing itself is uniform
(`randperm(E)[:8]` per token), which is easier for every grouped implementation
than a real skewed router.

## Family A — Qwen3-4B dense MLP block

`gate_up` (N=19456, K=2560) → silu·mul → `down` (N=2560, K=9728), activation
quantize inside the graph. MXFP8 uses the fused
`silu_chunk_mul_quantize_1x32_fp8` (no BF16 intermediate); BSFP8 uses the
1×128 quantize and a `torch.compile`d silu·mul. `model ms` = µs × 36 layers / 1000.

| M | BF16 (torch) µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | model ms | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos | model ms | cuBLAS scaled_mm MXFP8 + reference quantize µs | cuBLAS scaled_mm MXFP8 + torch.compile quantize µs |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 33.04 | 20.54 | 7 | 0.9978 | 0.74 | 18.48 | 8 | 0.9978 | 0.67 | 86.10 | 32.83 |
| 2 | 36.96 | 20.54 | 15 | 0.9979 | 0.74 | 18.48 | 16 | 0.9979 | 0.67 | 90.20 | 32.81 |
| 4 | 36.92 | 20.53 | 29 | 0.9979 | 0.74 | 18.49 | 32 | 0.9979 | 0.67 | 92.21 | 32.84 |
| 8 | 36.97 | 20.55 | 58 | 0.9979 | 0.74 | 18.48 | 65 | 0.9979 | 0.67 | 96.41 | 32.88 |
| 16 | 37.17 | 22.57 | 106 | 0.9978 | 0.81 | 18.50 | 129 | 0.9978 | 0.67 | 100.50 | 32.83 |
| 32 | 38.85 | 24.63 | 194 | 0.9979 | 0.89 | 22.17 | 216 | 0.9979 | 0.80 | 104.55 | 34.88 |
| 64 | 40.61 | 30.78 | 311 | 0.9979 | 1.11 | 26.69 | 358 | 0.9979 | 0.96 | 118.64 | 39.11 |
| 128 | 49.23 | 36.91 | 518 | 0.9979 | 1.33 | 31.47 | 608 | 0.9979 | 1.13 | 128.92 | 34.99 |
| 256 | 55.41 | 42.83 | 893 | 0.9979 | 1.54 | 37.48 | 1021 | 0.9979 | 1.35 | 159.12 | 43.09 |
| 512 | 82.17 | 57.57 | 1329 | 0.9979 | 2.07 | 51.69 | 1480 | 0.9979 | 1.86 | 211.03 | 57.74 |
| 1024 | 140.45 | 86.33 | 1772 | 0.9979 | 3.11 | 79.64 | 1921 | 0.9979 | 2.87 | 321.28 | 90.66 |
| 2048 | 277.63 | 151.29 | 2023 | 0.9979 | 5.45 | 139.05 | 2201 | 0.9979 | 5.01 | 569.38 | 151.13 |
| 4096 | 571.12 | 284.79 | 2149 | 0.9979 | 10.25 | 257.14 | 2380 | 0.9979 | 9.26 | 1070.55 | 282.34 |
| 8192 | 1168.58 | 600.50 | 2038 | 0.9979 | 21.62 | 554.68 | 2207 | 0.9979 | 19.97 | 2062.15 | 593.21 |

Reading: MXFP8 is the faster fso path at every M, by 8–18 % over BSFP8. That
gap is glue, not GEMM — the dense tables in [`../gemm/sm100.md`](../gemm/sm100.md)
show the two dtypes running the same kernel within a timer step, and the
difference here is the fused silu-and-quantize stage the MXFP8 block has and
the BSFP8 block does not. Against the fused cuBLAS chain the MXFP8 block is
1.47–1.78× faster through the decode band (M ≤ 64) and 1.07–1.15× from
M = 128 up; against the unfused cuBLAS chain it is 3.7–5.4× faster at every M,
which is the price of the reference quantize rather than a property of cuBLAS's
GEMM. Against the BF16 reference the block goes from 1.79× at M = 1 to 2.11× at
M = 8192. Two shapes of the grid are worth naming. The MXFP8 column barely
moves from M = 1 to M = 16 — 18.48, 18.48, 18.49, 18.48 and 18.50 µs for a
sixteenfold growth in token count — and steps up one replay tick at M = 32,
because in that band the block is bound by its chain of kernel launches rather
than by arithmetic; from this run both of its GEMMs are served there by the
M ≤ 32 decode row of [`../gemm/sm100.md`](../gemm/sm100.md) (`gate_up` by the
persistent kernel, `down` by the in-cluster split-K kernel), which is where the
block's six-microsecond drop against the previous run comes from. The dense
wave-tile rule starts only at M = 64, so the M = 64 and M = 128 cells are the
two where the block's `gate_up` is routed by how many CTAs a narrower N tile
puts on the 148 SMs. And the M = 4096 and M = 8192 rows of every column are
the ones the unlocked clock reaches — read M = 4096 with the ±3 % band rather
than ±1 %, and read M = 8192 with the wider band the scatter measurement in
[`../gemm/sm100.md`](../gemm/sm100.md) established, which is about 8 % and
applies to the pure-cuBLAS comparator column as well as to the fso ones.

## Family B — Qwen3-30B-A3B routed MoE layer (E=128, top-8, moe_inter 768)

fso: masked-slab MXFP8 layer (`m_cap` rows per expert). How many kernels the
captured graph holds depends on the band, because both of the stages a grouped
GEMM can absorb are now absorbed on both routes. In the decode band, where few
enough experts can hold a row for the slot-bound route to be taken, each
grouped GEMM consumes the packed active-expert list that the routing kernel
emits in the launch that already computes the histogram it is a compaction of,
so neither GEMM needs an argument-preparation kernel ahead of it; and the first
of the two carries a fused SwiGLU epilogue of its own, so the separate
SwiGLU-and-quantize kernel is gone there as well. The graph is five kernels: the
routing builder, the gather-quantize, the two grouped GEMMs and the weighted
combine. Above that band the dispatcher returns to the pointer-array cascade,
which does need one argument-preparation kernel per grouped GEMM, and the
pointer-array form of the fused FC1 removes the SwiGLU kernel there, so the
graph is seven kernels. Counted with nsys on this run, the graph holds five kernels
per replay at M = 1 and M = 8 and seven at M = 64, M = 1024 and M = 4096, and the
routing kernel is asked for the packed list only at the M where the slot route
is taken (M ≤ 32).
`model ms` = µs × 48 / 1000. M ≤ 4096 for the masked slab layout; M = 96 is an
extra measured row.

| M | active experts | m_cap | fso block µs | TFLOPS | weight GB/s | cos | model ms | torch scaled_grouped_mm MXFP8 µs (routed) | torch _grouped_mm BF16 µs (routed) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8 | 4 | 16.0 | 4.7 | 2356 | 0.9975 | 0.77 | 77.9 | 63.6 |
| 2 | 16 | 4 | 20.6 | 7.3 | 3673 | 0.9980 | 0.99 | 96.4 | 92.1 |
| 4 | 28 | 4 | 35.2 | 8.6 | 3753 | 0.9979 | 1.69 | 97.8 | 104.6 |
| 8 | 47 | 8 | 53.7 | 11.3 | 4133 | 0.9979 | 2.58 | 114.6 | 138.1 |
| 16 | 82 | 16 | 83.1 | 14.5 | 4655 | 0.9979 | 3.99 | 145.2 | 201.9 |
| 32 | 106 | 32 | 102.9 | 23.5 | 4861 | 0.9979 | 4.94 | 182.3 | 261.4 |
| 64 | 125 | 64 | 123.5 | 39.1 | 4776 | 0.9979 | 5.93 | 204.4 | 299.7 |
| 96 | 126 | 96 | 125.3 | 57.9 | 4746 | 0.9979 | 6.01 | — | — |
| 128 | 128 | 128 | 127.9 | 75.6 | 4723 | 0.9979 | 6.14 | 219.7 | 307.6 |
| 256 | 128 | 256 | 133.8 | 144.5 | 4515 | 0.9979 | 6.42 | 233.7 | 320.9 |
| 512 | 128 | 512 | 139.5 | 277.0 | 4328 | 0.9979 | 6.70 | 259.1 | 341.3 |
| 1024 | 128 | 1024 | 152.7 | 506.1 | 3954 | 0.9979 | 7.33 | 367.6 | 424.5 |
| 2048 | 128 | 2048 | 191.2 | 808.5 | 3158 | 0.9979 | 9.18 | 452.5 | 482.1 |
| 4096 | 128 | 4096 | 279.6 | 1106.2 | 2161 | 0.9979 | 13.42 | 633.1 | 587.6 |
| 8192 | 128 | — | — | — | — | — | — | 1047.8 | 990.9 |

Reading: fso is ahead of both torch comparators at every M where it has a cell.
Against `scaled_grouped_mm` in MXFP8 the margin is 4.7–4.9× at M = 1–2 and
2.1–2.8× at M = 4–8, narrows to 1.7–1.9× through M = 16–512 where both
implementations are bandwidth-shaped, and widens again to 2.3–2.4× from
M = 1024 up. Against `_grouped_mm` in BF16 — which does no quantization at all
and reads twice the weight bytes — the margin is 4.0–4.5× at M = 1–2 and stays
between 2.1× and 3.0× from M = 4 up. The fso block itself is nearly flat from
M = 32 to M = 256 (102.9 → 133.8 µs for an eightfold token count), because in
that band the layer is paying for the weights of the active experts rather than
for arithmetic; `weight GB/s` peaks at 4861 and falls to 2161 by M = 4096, which
is where the slab stops fitting in L2 between replays. The two ends of the grid
are where the MXFP8 round's work shows, and they show for different reasons. At
M = 1–2 almost nothing in the block is GEMM, so what the layer costs is the
chain of small kernels; the slot-bound route removes two of them outright,
because each grouped GEMM is indexed by the packed expert list the routing
kernel already produced and needs no argument-preparation launch of its own,
and from this run its FC1 carries the fused SwiGLU epilogue as well, so the
separate SwiGLU-and-quantize kernel is gone there too and the captured graph is
five kernels. At M ≥ 1024 the two stages that grow with the token count are the
activation quantize and the FC1 output, and both were folded into a neighbour:
each token is quantized once and scattered to its top-k destinations instead of
being quantized once per destination, and FC1's SwiGLU and requantize are done
in the grouped GEMM's own epilogue rather than in a kernel that first writes a
BF16 intermediate. Between those ends, the M = 64–1024 cells moved in this run
for a third reason: the pointer-array cascade's direct-store epilogue is now
partitioned so that each thread stores one row and 64 columns whatever the N
tile, which lets `moe.down` take a 256-wide tile it could not afford before
(the grouped-kernel table in [`../gemm/sm100.md`](../gemm/sm100.md) shows that
column alone moving). The M = 8192 row has no fso cell: the `G × m_cap` masked
slab does not fit, the same limit sm_120 has.

## Family C — Qwen3.5-35B-A3B MoE block (E=256, top-8, moe_inter 512, shared expert 512)

fso routed: the same masked-slab MXFP8 layer as Family B. fso routed + shared:
that layer plus the shared expert (`gate_up` 1024×2048 → fused silu-quantize →
`down` 2048×512, MXFP8) and the residual add, in one graph. Block TFLOPS, GB/s,
cos and `model ms` (× 40 layers) are for the routed + shared column; `shared
expert µs` is the difference between the two fso columns. The comparators are
published for both blocks.

| M | active experts | fso routed µs | fso routed + shared µs | shared expert µs | TFLOPS (block) | weight GB/s (block) | cos (block) | model ms (block) | torch scaled_grouped_mm MXFP8 µs (routed) | torch _grouped_mm BF16 µs (routed) | torch scaled_grouped_mm MXFP8 µs (routed + shared) | torch _grouped_mm BF16 µs (routed + shared) |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8 | 14.4 | 26.7 | 12.3 | 2.1 | 1060 | 0.9979 | 1.07 | 88.5 | 65.7 | 110.9 | 80.0 |
| 2 | 16 | 18.5 | 29.2 | 10.8 | 3.9 | 1830 | 0.9982 | 1.17 | 102.5 | 78.9 | 124.8 | 90.2 |
| 4 | 30 | 26.2 | 39.5 | 13.3 | 5.7 | 2468 | 0.9979 | 1.58 | 96.0 | 88.4 | 118.6 | 102.4 |
| 8 | 58 | 48.3 | 61.2 | 12.8 | 7.4 | 3035 | 0.9978 | 2.45 | 115.3 | 133.2 | 138.5 | 143.6 |
| 16 | 100 | 73.7 | 86.4 | 12.7 | 10.5 | 3677 | 0.9979 | 3.46 | 141.9 | 179.6 | 165.3 | 192.4 |
| 32 | 161 | 109.1 | 123.1 | 14.0 | 14.7 | 4139 | 0.9979 | 4.92 | 195.0 | 260.9 | 217.2 | 272.0 |
| 64 | 221 | 140.5 | 155.0 | 14.6 | 23.4 | 4504 | 0.9978 | 6.20 | 233.4 | 334.1 | 252.9 | 343.1 |
| 96 | 242 | 154.2 | 169.9 | 15.7 | 32.0 | 4500 | 0.9979 | 6.79 | — | — | — | — |
| 128 | 252 | 159.2 | 174.6 | 15.4 | 41.5 | 4559 | 0.9979 | 6.98 | 263.7 | 377.6 | 280.0 | 390.1 |
| 256 | 256 | 166.8 | 182.0 | 15.2 | 79.6 | 4442 | 0.9979 | 7.28 | 278.5 | 391.0 | 296.2 | 403.5 |
| 512 | 256 | 173.3 | 189.5 | 16.2 | 153.0 | 4266 | 0.9979 | 7.58 | 304.5 | 412.2 | 322.9 | 424.2 |
| 1024 | 256 | 187.5 | 204.8 | 17.3 | 283.2 | 3948 | 0.9979 | 8.19 | 396.3 | 495.8 | 418.2 | 511.5 |
| 2048 | 256 | 211.8 | 235.9 | 24.0 | 491.6 | 3427 | 0.9979 | 9.44 | 493.5 | 550.2 | 524.6 | 571.0 |
| 4096 | 256 | 284.8 | 323.2 | 38.4 | 717.6 | 2502 | 0.9979 | 12.93 | 663.1 | 629.3 | 699.5 | 661.7 |
| 8192 | 256 | — | — | — | — | — | — | — | 1002.6 | 899.6 | 1070.8 | 964.1 |

Reading: on the routed layer fso is 1.7–6.1× faster than `scaled_grouped_mm`
MXFP8 and 2.2–4.6× faster than `_grouped_mm` BF16 across the whole grid, and
the routed + shared block holds a narrower but similar lead against its own
comparators (against `scaled_grouped_mm`, 4.2× at M = 1 and 2.2× at M = 4096).
The shared expert costs a flat 10.8–17.3 µs from M = 1 to M = 1024 — its two
GEMMs are small enough to sit at the replay floor, as the dense rows in
[`../gemm/sm100.md`](../gemm/sm100.md) show — and only starts to scale above
that: 24.0 µs at M = 2048 and 38.4 µs at M = 4096, which is 12 % of the block
there. The two torch columns cross over:
BF16 is the slower comparator through the middle of the grid but the faster one
at M = 1–4 and again at M = 4096, because its per-call overhead is smaller at
decode and its extra weight traffic only starts to bite once the block is
bandwidth-bound. What separates this block from the 2026-09-17 run's is the
same mechanisms Family B shows, and they apply here unchanged even though this
family's `moe.gate_up` is a different shape: the slot-bound route at the decode
end, from this run with the fused SwiGLU epilogue on its FC1 as well, the
quantize-once gather, the fused SwiGLU epilogue on the pointer-array FC1 from
M = 64 upward, and the partitioned direct-store epilogue, whose re-derived tile
rule is what moved this family's M = 512–2048 cells (its `moe.down` kernel cell
is the one that changed pick; see the grouped-kernel table in
[`../gemm/sm100.md`](../gemm/sm100.md)). As in Family B the M = 8192 fso cells
are absent for the masked-slab size limit.
