# GEMM performance — sm_100 / sm_103 (NVIDIA B300)

Frozen reference numbers for the block-scaled GEMM paths on sm_103. Protocol,
FLOPs conventions, shape families, M grids and the clock note: [`../README.md`](../README.md).
Tables are generated from `tests/baselines/` jsonl — do not hand-edit rows.

## Provenance and status (2026-09-23)

**Measured.** Every table in this file comes from one run on 2026-09-23,
on the B300 the maintainer granted for the day (PCI `E6:00.0` of the Kubernetes
pod named in the Environment table, the same card as the 2026-09-22 run this
one replaces). The tree measured was commit `f790fe3` plus the uncommitted
MXFP8 change set, built once as a union in the virtual environment that
carries `nvidia-cutlass-dsl` 4.8.0 and verified before the run: the twelve
unit suites and the JIT-isolation lint pass on this device, including the
three this change set adds cases to (`test_mxfp8_grouped`, `test_cuda_graph`,
`test_mxfp8_decode_sm100`) and `test_moe_routing_threads`, which reports `ran:
single-CTA, multi-CTA, skipped: none`, while the three sm_90-only suites now
print their SKIP line and exit 0 (the device guard this change set gives
them); the suites pass again with each of `FSO_FC1_FUSED=0`,
`FSO_GROUPED_SLOT=0` and `FSO_DISABLE_DECODE_DSL=1` set; `compute-sanitizer
--tool memcheck` reports no error on a whole Family B MoE layer call at
M ∈ {1, 8, 48, 64, 1024} nor on dense `linear_mxfp8` `wo` and `wqkv` at
M = 40 and 64, the band the decode row gained; and every family was measured
against the previous tree in the same session, alternating trees under one GPU
lock, two passes per tree on every family. 8 cells of that comparison landed outside the acceptance band.
Five of them are in fso columns and three in comparator columns that contain no fso kernel (torch BF16 `cubic` at 8192³ and `down` at M = 2048, and the MLP block's cuBLAS `scaled_mm` column with the `torch.compile`d quantize at M = 64, which read 41.1 µs against 39.3 in six passes; the block's own two fso columns are unchanged there to 0.1 %, and with the decode row switched off in the same worker process the comparator reads 39.1–39.3 again, so what moved is a process-level interaction with the row's compiled kernels rather than a kernel of this tree). Two fso cells are Family C `gdn_in_proj` in MXFP8 at M = 128 and M = 256, the device's known unstable cells: the dense change of this set stops at M = 64, the previous run's nsys showed the identical cuBLAS `nvjet` kernel there on both trees, and over six passes per tree the medians read 6.82 against 6.59 µs at M = 128 and 8.51 against 8.67 at M = 256 while each tree scatters 8–10 % on its own. One is the cubic 8192³ BSFP8 scatter cell, whose six-pass medians agree to 0.5 % (341.2 against 343.0 µs) around single 360 µs outliers on both trees. The last two are Family A `gate_up` in BSFP8 at M = 1 and M = 2, where both trees' readings straddle the 8.3 and 10.3 µs replay ticks over six passes (8.80–10.31 against 8.78–10.28 at M = 1, 8.30–10.30 against 8.35–10.27 at M = 2) while the MXFP8 column of the same cells, which runs the same decode kernel, reads 8.27 µs on both trees to two decimals — a tick straddle, not a movement.

The device has no clock lock available, so **every row in this file is an
unlocked-clock number** and carries the caveats in the "Unlocked device"
section below.

What this run contains that the previous one did not, stated as mechanisms:

- **A slot-bound decode route on the grouped MXFP8 GEMM, reachable from the MoE
  layer.** The pointer-array grouped kernel spends one CTA-tile on every
  (group, row-tile) pair the masked slab has, which in the decode band is
  almost all padding: at top-8 routing only a handful of the expert set can
  hold a row. A second grouped instance, in swap orientation, indexes its grid
  by a packed list of the experts that actually hold rows, and skips a tile
  whose slot is dead. Which route a call takes is decided by the host-static
  bound `max_active_groups = min(M · topk, E)` that every call site now passes:
  while the experts that can hold a row are at most three eighths of the expert
  set, the slot route is taken. The bound is host static for a given
  (M, topk, E), so a graph captured at one M stays valid for every routing draw
  at that M.
- **The packed active-expert list emitted by the routing kernel.** Building
  that list used to be a one-block kernel of the slot route's own, and before
  that each grouped GEMM needed an argument-preparation kernel. The routing
  builder already computes the histogram the list is a compaction of, so it now
  emits the list in the same launch and the slot route launches nothing but the
  GEMM. Two one-block launches per MoE layer disappear.
- **One activation quantize per token instead of one per destination.** The
  grouped activation quantizer filled each destination row of the
  `[E, m_cap, hidden]` slab by reading the token routed there, so a token
  chosen by eight experts was read and quantized eight times. It now quantizes
  each token once and scatters the result to its top-k destinations, which
  divides the read traffic and the quantize arithmetic of that stage by the
  top-k factor.
- **A fused SwiGLU epilogue on the first MoE GEMM.** FC1 used to write a
  BF16 `[E, m_cap, 2·I]` intermediate that a separate kernel then read, halved
  with silu·mul and requantized to MXFP8. A grouped instance whose epilogue
  does the silu·mul and the requantize emits the MXFP8 FC2 input directly, so
  both the intermediate and that kernel disappear. It needs gate and up weight
  rows interleaved, which is a load-time decision the library owns, and it is
  not used where the swap-orientation decode route is taken, because that
  geometry puts a token's gate and up halves in different lanes.
- **An equal-CTA tie-break in the dense wave-tile rule.** The rule picks the
  tile whose grid puts the most CTAs on the machine without spilling into a
  second wave, and a 1-SM (TileM 128) and a 2-SM (TileM 256) form of the same N
  width can tie on that count. The tie now goes to the 2-SM form, which covers
  256 rows per tile instead of 128 and therefore makes half as many passes over
  the weight matrix. Measured alone the two forms are indistinguishable,
  because an isolated dense cell replays one GEMM with its whole working set
  resident in this part's L2; inside the fused MLP block they are not, because
  `down` runs straight after `gate_up` has swept L2 and every extra pass over
  its weight is HBM traffic. The 2-SM 192-wide instance was added for this.

| section | baseline jsonl | status |
|---|---|---|
| Family A dense tables | `tests/baselines/gemm_sm100_qwen3_4b.jsonl` (pass 1 of two) | **accepted baseline** |
| Family A MLP block forward | `tests/baselines/gemm_sm100_qwen3_4b_mlp_fwd.jsonl` (pass 1 of two) | **accepted baseline**, published in [`../layer/sm100.md`](../layer/sm100.md) |
| Family B dense projections | `tests/baselines/gemm_sm100_qwen3_30a3_dense.jsonl` (pass 1 of two) | **accepted baseline** |
| Family B grouped MoE layer + kernels | `tests/baselines/perf_moe_qwen3_30a3_b300.jsonl` (pass 1 of two) | **accepted baseline** |
| Family C dense projections | `tests/baselines/gemm_sm100_qwen3_35a3_dense.jsonl` (pass 1 of two) | **accepted baseline** |
| Family C grouped MoE layer + kernels | `tests/baselines/perf_moe_qwen3_35a3_b300.jsonl`, `tests/baselines/perf_moe_qwen3_35a3_shared_b300.jsonl` (pass 1 of two) | **accepted baseline** |

Artifacts: `/data/bench-runs/b300_final7_20260923/` on the H200 box, mirrored at
`/home/fish-dev/bench-runs/b300_final7_20260923/` on the pod; the subtasks
behind this run — the slot route and its layer wiring, the quantize-once
gather, the per-tile dead-slot skip, the fused SwiGLU epilogue and the packed
slot list — are in `/data/bench-runs/b300_mxfp8_20260917/` under `M-I2/`,
`M-I3/`, `M-Q1/`, `M-I5/`, `M-E2/` and `M-I1/`, and the analysis behind the
dense tie-break is in `/data/bench-runs/b300_head_vs_wt_20260921/R2/`.

Not published from these baselines, by policy: the Family A jsonl also carries
the legacy `gate` rows (N=9728) and a cubic M = N = K sweep, neither of which
is part of a shape family ([`../README.md`](../README.md) §3), and every dense
record also carries a cuBLAS `scaled_mm` column, which is a comparison and so
belongs only in [`../layer/sm100.md`](../layer/sm100.md). They stay in the
baseline files so a later decision can use them without a new run.

Earlier status of this file, for the record: until 2026-09-05 it was a
skeleton, because the B300 hosts were unreachable, and the archived single
`perf.md` carried B300 rows taken without a clock lock and with no preserved
jsonl, none of which were carried over. The first generated tables here were
measured on 2026-09-15 on a different card of the same pod (PCI `86:00.0`,
UUID `GPU-b2bdc48e-…`) and were replaced wholesale on 2026-09-17 by a run on
PCI `06:00.0`, which the run above in turn replaced wholesale. Each replacement
was whole rather than a merge because a number from one card and a number from
another are not comparable under this protocol and mixing them would hide that.
How much the card alone is worth was measured here: the torch comparator
columns of [`../layer/sm100.md`](../layer/sm100.md), which contain no fso
kernel and cannot move with a library change, read about 1.8 % faster on this
card than the same columns did on `06:00.0`.

## Environment

| field | value |
|---|---|
| device | NVIDIA B300 SXM6 AC (sm_103), 148 SMs, 275040 MiB HBM3e, 126.5 MiB L2, PCI `00000000:E6:00.0`, UUID `GPU-325138bf-36da-94f6-7696-a628359f2e4b` |
| host | Kubernetes pod `fish-dev-5f7c8f8bbf-hlvt4`, Debian 12, 256 cores |
| clock | **no lock available** — the pod cannot set one; with persistence mode on, the card ramps to ~2032 MHz flat under load. Every cell ran with `FSO_BENCH_WARM_MS=300`; every row is labelled `unlocked` |
| driver | 595.84 |
| Python / PyTorch / CUDA runtime | 3.12.13 / 2.11.0+cu130 / 13.0 |
| build toolkit | nvcc 13.0 (V13.0.48), `ARCH=10.0f` (the family target that serves B200 and B300); the device reports compute capability (10, 3) |
| CUTLASS | 4.4.2 (`3rdparty/cutlass` @ tag `v4.4.2`) for the ahead-of-time kernels; `nvidia-cutlass-dsl` **4.8.0** in the virtual environment, which is what the CuTe-DSL rows need. The M ≤ 64 dense decode row requires the `sm100` extra at 4.5.0 or newer and disables itself below that floor, so on the pinned 4.4.2 environment the dense decode cells of this file read what the tiers behind the row give (the cuBLAS tier and the C++ cascade, one replay tick more at M ≤ 32 on every shape but `down`) |
| kernels | ahead-of-time CUTLASS block-scaled tcgen05 instances (dense and grouped); no NVRTC JIT on this arch |
| fso commit | `f790fe3` plus the uncommitted MXFP8 change set — the slot route's row-capacity clause made per kernel (the plain slot kernel runs to its full 64-wide token tile, the fused-SwiGLU slot kernel to one 32-column epilogue chunk), the pointer-array grouped route reading its per-group problem shapes from the routing kernel with its tensor-set arrays written once into a static arena instead of an argument-preparation kernel per GEMM, and the dense CuTe-DSL decode row extended from M ≤ 32 to M ≤ 64 with a 64-wide token tile on the 2-CTA 256-row weight tile. All of it lands squashed as **this commit**; its hash replaces this note when the squash lands |
| measured | 2026-09-23, one session under a single GPU lock: MLP block 04:49:23–04:51:12 UTC, Family B grouped and layer 04:56:48–04:58:27, Family C grouped and layer 05:03:49–05:05:46, Family C routed + shared 05:10:07–05:10:55, Family B dense 05:14:11–05:15:59, Family A dense 05:24:28–05:29:55, Family C dense 05:45:17–05:50:24, torch comparators 06:00:33–06:18:30 |

### Unlocked device — how to read these rows

The B300 in this pod cannot be clock-locked, so the comparability rule in
[`../README.md`](../README.md) §5 is satisfied only by the warm-up protocol and
the labelling, not by a lock. Four consequences, all measured on this device:

- **Cells at M ≥ 4096 swing by about ±3 %, not ±1 %.** The MLP block's BF16
  cell at M = 4096 was measured nine times on identical code on 2026-09-15 and
  spans 555.1 to 586.0 µs, a 5.6 % spread. Rows in that band bound the kernel
  loosely; a difference smaller than 3 % between two runs at M ≥ 4096 on this
  device is not a signal.
- **The M = 8192 cells of the MLP block are wider still, and the comparator
  columns prove it is the device.** On 2026-09-17 twelve independent worker
  processes measured that block's M = 8192 cell alone, alternating between two
  library builds. The BSFP8 column spanned 587.6 to 613.8 µs, a 4.4 % spread,
  and the two builds' medians differed by 0.1 %. The pure cuBLAS comparator
  column in the same passes — which contains no fso kernel at all and therefore
  cannot move with a library change — spanned 586.4 to 607.5 µs, the same 3.5 %.
  A change read off the M = 8192 row of [`../layer/sm100.md`](../layer/sm100.md)
  is inside the noise of the cell unless it is larger than about 8 %, so that
  row is a published number, not an acceptance unit.
- **The clock droops under sustained replay of the longest cells.** Replaying
  the M = 8192 MLP graph continuously takes the SM clock from 2032 MHz down to
  1822–1980 MHz over twelve blocks of 50 replays, with board power rising from
  228 W to 538 W; the same experiment at M = 1024 shows no droop at all. The
  M = 8192 rows are therefore measured at a lower clock than the rest of the
  grid, which is part of why their TFLOPS stop rising.
- **Small cells quantise in ~2.05 µs steps — the graph-replay tick.** Every
  small replay median on this device is an integer multiple of one tick: a
  least-squares fit over eighteen distinct medians of one dense run gives
  2.0604 µs with a worst residual of 0.135 µs (0.65 %), so the readings land on
  4.16 / 6.20 / 8.24 / 10.29 / 12.34 / 14.39 / 16.44 µs and nowhere between.
  Two things follow. A kernel improvement smaller than one tick cannot show at
  all, and when it does show it shows as a whole tick, so a cell that flips by
  one step reads as a 20–25 % change and is not one. Below M ≈ 256 most of the
  dense grid is in that regime, so those rows should be read as "at the replay
  floor" rather than compared as percentages; one cell moving one tick is not a
  signal, a run of consecutive M cells moving together is.

### Small-M caveat (all tables in this file)

Graph replay re-runs one cell's kernels with the same weights and the bench
does not flush L2, so at small M the active weights stay in the device's
126.5 MiB L2 and the µs undercut the DRAM floor. A `weight GB/s` above the
device's HBM bandwidth is L2-fed by definition — in the grouped kernel tables
below that is visible as the 3.8–5.4 TB/s plateaus, against a measured
streaming ceiling of about 6.2 TB/s. These rows bound the kernel, not the
serving cost, which rotates every layer's weights through DRAM
([`../README.md`](../README.md) §8).

### dtypes on sm_100 / sm_103

- **BF16** is the unquantized reference column (`torch.nn.functional.linear`),
  not a competitor; it is here so the FP8 columns have a truth to be measured
  against and a speed to be read against.
- **BSFP8** is block-scaled FP8 with 1×128 activation and 128×128 weight
  scales. On this arch there is no separate block-FP8 kernel: the wrapper
  expands each 1×128 scale onto the four MXFP8 1×32 tiers it covers and calls
  the MXFP8 kernel, so the GEMM that runs and the bytes it reads are the same
  as the MXFP8 row's. The two FP8 columns therefore differ only by the
  expansion step and by measurement noise, which is why they agree to a timer
  step nearly everywhere below. K % 128 and N % 128 are required.
- **MXFP8** is 1×32 UE8M0 block scales. The dense dispatcher is a three-tier
  router: cuBLAS `scaled_mm` fed fso's own packed scale buffer, the C++ CUTLASS
  cascade, and a CuTe-DSL persistent kernel. Its large-M rules were rewritten
  on 2026-09-15 — wide-N shapes (`tiles_n > 32`) go to the cuBLAS tier at every
  M, narrow-N shapes above M = 1024 go there unless the shape is long-K with a
  deep enough cascade grid, and square shapes above M = 1024 stay on the DSL
  tier's cubic table. On 2026-09-17 the wave-tile rule described above was
  added ahead of all three tiers for the band 64 ≤ M ≤ 1024: where a narrower N
  tile would put more CTAs on the machine without starting a second wave, the
  cell goes to the C++ cascade with that tile and the other two tiers decline
  it. Every cell outside the rule's bounds routes exactly as before.
- **MoE** rows are MXFP8 on the sm_100 grouped path: a CUTLASS pointer-array
  block-scaled kernel over the masked slab layout, with the per-group argument
  arrays rebuilt on device before each GEMM so one capture serves any routing.
  The captured layer is eight kernels (the six of the sm_120 layer plus one
  argument-preparation kernel per grouped GEMM); that preparation kernel is
  split around a grid-dependency barrier and both it and the GEMM are launched
  as programmatic dependent launches, so the routing-independent half overlaps
  the kernel before it. The tile rules are cascade v2: TileM = 128 in a 1×1
  cluster with TileK = 128 everywhere, a 256-wide N tile on the long-mainloop
  projection except at `m_cap ≤ 4`, the direct-store epilogue on the
  short-mainloop projection while the store is still a sliver, and — new on
  2026-09-17 — a 192-wide direct-store tile on the long-mainloop projection
  when N is a multiple of 192, the live rows per tile are still few, and the
  192-wide grid fills its last wave better than the 256-wide one. On the four
  projections in this file that last rule fires on Family B's `moe.gate_up`
  only. M = 8192 is absent from the grouped grid for the same reason as on
  sm_120: the `G × m_cap` masked slab does not fit.

`µs` is the CUDA-graph replay median (50 replays × 3 repeats, median of three);
`cos` is cosine similarity against the BF16 result on the same inputs;
TFLOPS = `2·M·N·K / µs`.

## Family A — Qwen3-4B dense

### `wqkv` (N=6144, K=2560)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8.23 | 6.18 | 5 | 0.9993 | 6.20 | 5 | 0.9993 |
| 2 | 6.23 | 6.18 | 10 | 0.9993 | 6.20 | 10 | 0.9993 |
| 4 | 6.22 | 6.20 | 20 | 0.9993 | 6.20 | 20 | 0.9993 |
| 8 | 6.21 | 6.19 | 41 | 0.9993 | 6.20 | 41 | 0.9993 |
| 16 | 6.19 | 6.20 | 81 | 0.9993 | 6.20 | 81 | 0.9993 |
| 32 | 6.19 | 6.21 | 162 | 0.9993 | 6.21 | 162 | 0.9993 |
| 64 | 8.23 | 6.48 | 311 | 0.9993 | 6.21 | 324 | 0.9993 |
| 128 | 8.24 | 8.23 | 489 | 0.9993 | 6.37 | 632 | 0.9993 |
| 256 | 10.29 | 8.24 | 978 | 0.9993 | 8.24 | 978 | 0.9993 |
| 512 | 12.45 | 10.29 | 1565 | 0.9993 | 10.29 | 1565 | 0.9993 |
| 1024 | 22.54 | 14.41 | 2235 | 0.9993 | 14.41 | 2235 | 0.9993 |
| 2048 | 41.07 | 24.62 | 2616 | 0.9993 | 24.61 | 2618 | 0.9993 |
| 4096 | 77.65 | 45.03 | 2862 | 0.9993 | 45.15 | 2854 | 0.9993 |
| 8192 | 148.24 | 85.29 | 3021 | 0.9993 | 84.48 | 3050 | 0.9993 |

### `wo` (N=2560, K=4096)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8.27 | 6.19 | 3 | 0.9993 | 6.18 | 3 | 0.9993 |
| 2 | 8.26 | 6.19 | 7 | 0.9993 | 6.18 | 7 | 0.9993 |
| 4 | 8.25 | 6.20 | 14 | 0.9993 | 6.17 | 14 | 0.9993 |
| 8 | 8.33 | 6.19 | 27 | 0.9993 | 6.20 | 27 | 0.9993 |
| 16 | 8.26 | 6.20 | 54 | 0.9993 | 6.21 | 54 | 0.9993 |
| 32 | 8.26 | 8.23 | 82 | 0.9993 | 8.24 | 81 | 0.9993 |
| 64 | 8.28 | 8.25 | 163 | 0.9993 | 8.25 | 163 | 0.9993 |
| 128 | 8.25 | 8.26 | 325 | 0.9993 | 8.28 | 324 | 0.9993 |
| 256 | 10.29 | 8.27 | 649 | 0.9993 | 8.27 | 649 | 0.9993 |
| 512 | 12.34 | 10.27 | 1045 | 0.9993 | 8.28 | 1297 | 0.9993 |
| 1024 | 16.29 | 12.35 | 1738 | 0.9993 | 12.35 | 1739 | 0.9993 |
| 2048 | 28.63 | 18.64 | 2305 | 0.9993 | 18.66 | 2302 | 0.9993 |
| 4096 | 52.64 | 31.90 | 2693 | 0.9993 | 31.61 | 2717 | 0.9993 |
| 8192 | 104.21 | 57.53 | 2986 | 0.9993 | 57.60 | 2983 | 0.9993 |

### `gate_up` (N=19456, K=2560)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 10.52 | 9.05 | 11 | 0.9993 | 8.26 | 12 | 0.9993 |
| 2 | 10.77 | 8.46 | 24 | 0.9993 | 8.29 | 24 | 0.9993 |
| 4 | 11.06 | 8.28 | 48 | 0.9993 | 8.24 | 48 | 0.9993 |
| 8 | 11.72 | 8.27 | 96 | 0.9993 | 8.27 | 96 | 0.9993 |
| 16 | 12.34 | 10.13 | 157 | 0.9993 | 8.26 | 193 | 0.9993 |
| 32 | 12.48 | 9.19 | 347 | 0.9993 | 8.92 | 357 | 0.9993 |
| 64 | 13.38 | 10.30 | 619 | 0.9993 | 10.28 | 620 | 0.9993 |
| 128 | 21.56 | 10.30 | 1238 | 0.9993 | 10.30 | 1238 | 0.9993 |
| 256 | 23.33 | 14.39 | 1773 | 0.9993 | 14.37 | 1774 | 0.9993 |
| 512 | 35.53 | 24.48 | 2084 | 0.9993 | 23.96 | 2128 | 0.9993 |
| 1024 | 65.04 | 38.98 | 2617 | 0.9993 | 38.95 | 2619 | 0.9993 |
| 2048 | 119.56 | 69.79 | 2923 | 0.9993 | 69.65 | 2929 | 0.9993 |
| 4096 | 230.35 | 131.76 | 3097 | 0.9993 | 131.29 | 3108 | 0.9993 |
| 8192 | 490.78 | 263.47 | 3097 | 0.9993 | 259.60 | 3143 | 0.9993 |

### `down` (N=2560, K=9728)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 10.31 | 6.22 | 8 | 0.9993 | 6.22 | 8 | 0.9993 |
| 2 | 10.31 | 6.23 | 16 | 0.9993 | 6.20 | 16 | 0.9993 |
| 4 | 10.30 | 6.21 | 32 | 0.9993 | 6.22 | 32 | 0.9993 |
| 8 | 10.27 | 6.24 | 64 | 0.9993 | 6.20 | 64 | 0.9993 |
| 16 | 10.30 | 8.24 | 97 | 0.9993 | 8.24 | 97 | 0.9993 |
| 32 | 10.29 | 10.31 | 155 | 0.9993 | 10.29 | 155 | 0.9993 |
| 64 | 10.30 | 12.34 | 258 | 0.9993 | 12.32 | 259 | 0.9993 |
| 128 | 12.35 | 14.40 | 443 | 0.9993 | 14.38 | 443 | 0.9993 |
| 256 | 16.43 | 14.41 | 885 | 0.9993 | 14.38 | 887 | 0.9993 |
| 512 | 22.57 | 14.58 | 1750 | 0.9993 | 14.53 | 1755 | 0.9993 |
| 1024 | 34.38 | 22.62 | 2255 | 0.9993 | 22.66 | 2251 | 0.9993 |
| 2048 | 65.30 | 39.19 | 2603 | 0.9993 | 39.24 | 2600 | 0.9993 |
| 4096 | 124.24 | 73.24 | 2786 | 0.9993 | 73.17 | 2788 | 0.9993 |
| 8192 | 246.70 | 132.59 | 3077 | 0.9993 | 132.45 | 3081 | 0.9993 |

Reading (Family A): the two FP8 columns are the same kernel on this arch and
read within one timer step of each other at almost every cell, which is the
expected result of expanding the 1×128 scales onto the MXFP8 tiers rather than
running a second kernel family. Below M = 256 every FP8 column of every shape
sits between 6 and 15 µs: the problems are too small to fill 148 SMs and the
readings are on the ~2.05 µs replay grid, so BF16 occasionally reads one step
*below* the FP8 paths there (`down` at M = 64 and 128, for instance) without
that meaning anything about the kernels. The M ≤ 32 band of every shape is
served by the CuTe-DSL decode row described above, as in the previous run: the
two FP8 columns read 6.18–6.23 µs on `wqkv`, `wo` and `down` and 8.25–8.34 µs
on `gate_up` at M = 1–16, one replay tick below what the cuBLAS tier and the
two-kernel split-K cascade give, with `gate_up` in BSFP8 at M = 1 (9.05 µs)
and M = 16 (9.19) and in MXFP8 at M = 32 (8.92) the cells whose two passes
straddled a tick. From this run the row also serves M = 33–64 on `wqkv`, `wo`
and `gate_up`, with its persistent kernel on a 64-wide token tile and the
2-CTA 256-row weight tile, where the cascade's wave tile served before; on this
family the replay column does not move there, because those cells were already
on the ~2.05 µs grid (`wqkv` 6.21, `wo` 8.24, `gate_up` 10.29 µs at M = 64 in
MXFP8 on both trees), while the profiler-free per-op slope the run directory's
standing rows take does — `wqkv` 5.62 → 4.35 µs, `wo` 6.96 → 5.38 and `gate_up`
7.94 → 7.17 at M = 64 — which is what makes the row level with FlashInfer's
cute-dsl kernels there. `down` (K = 9728, twenty weight tiles) is declined by
the row above M = 32 and keeps the cascade's split-K, reading 12.33 µs at M = 64
on both trees. The wave-tile rule now acts from M = 128: on this family it owns
`wqkv` at M = 128 and 512, `wo` at M = 128 and 1024, `gate_up` at M = 128, and
`down` at M = 1024, and at those cells what sets the reading is how many CTAs
the narrower N tile puts on the machine rather than the shape's arithmetic. The
signature in the table is that such a cell reads at the same floor as a far
smaller one: `wo` is at 8.23–8.28 µs in MXFP8 at M = 32, 64, 128, 256 and 512
alike. From M = 512 upward the FP8 paths separate from BF16,
by up to 1.9× depending on the shape and the M, with the widest margins at the
top of the grid: `gate_up` at M = 8192 is 477.57 µs in BF16 against 258.67 µs
in MXFP8, and `down` 248.81 against 133.37. The best dense rate in the family
is `gate_up` at M = 8192, 3155 TFLOPS in MXFP8 against 3144 in BSFP8 —
258.67 µs against 259.57 on the same kernel, which is what a 0.3 % scatter
looks like at the top of this grid.

### MLP block forward

Whole-block numbers (`gate_up` → silu·mul → `down` as one graph, with the
cuBLAS `scaled_mm` comparators) live in [`../layer/sm100.md`](../layer/sm100.md)
(Family A), generated from `tests/baselines/gemm_sm100_qwen3_4b_mlp_fwd.jsonl`.

## Family B — Qwen3-30B-A3B

### Dense projections `wqkv` (N=5120, K=2048), `wo` (N=2048, K=4096)

The attention head counts behind `wqkv` (32 query / 4 KV / head_dim 128) are
from the public config — verify against a local `config.json` when available.
`wo` is the same GEMM as Family C `wo` / `gdn.out_proj`; the three cells agree
within one timing step at every M.

| op | M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `wqkv` | 1 | 6.21 | 6.20 | 3 | 0.9993 | 6.19 | 3 | 0.9993 |
| `wqkv` | 2 | 6.22 | 6.17 | 7 | 0.9993 | 6.16 | 7 | 0.9993 |
| `wqkv` | 4 | 6.20 | 6.20 | 14 | 0.9993 | 6.19 | 14 | 0.9993 |
| `wqkv` | 8 | 6.21 | 6.18 | 27 | 0.9993 | 6.19 | 27 | 0.9993 |
| `wqkv` | 16 | 6.20 | 6.20 | 54 | 0.9993 | 6.20 | 54 | 0.9993 |
| `wqkv` | 32 | 6.20 | 6.19 | 108 | 0.9993 | 6.19 | 108 | 0.9993 |
| `wqkv` | 64 | 6.20 | 6.18 | 217 | 0.9993 | 6.20 | 217 | 0.9993 |
| `wqkv` | 128 | 6.22 | 6.20 | 433 | 0.9993 | 6.22 | 432 | 0.9993 |
| `wqkv` | 256 | 8.32 | 6.19 | 867 | 0.9993 | 6.20 | 866 | 0.9993 |
| `wqkv` | 512 | 10.30 | 10.29 | 1043 | 0.9993 | 8.27 | 1298 | 0.9993 |
| `wqkv` | 1024 | 16.33 | 12.34 | 1740 | 0.9993 | 12.31 | 1744 | 0.9993 |
| `wqkv` | 2048 | 27.66 | 18.53 | 2318 | 0.9993 | 18.47 | 2325 | 0.9993 |
| `wqkv` | 4096 | 53.41 | 31.55 | 2722 | 0.9993 | 31.46 | 2730 | 0.9993 |
| `wqkv` | 8192 | 99.04 | 58.98 | 2913 | 0.9993 | 58.65 | 2929 | 0.9993 |
| `wo` | 1 | 10.30 | 6.20 | 3 | 0.9993 | 6.19 | 3 | 0.9993 |
| `wo` | 2 | 10.29 | 6.18 | 5 | 0.9993 | 6.18 | 5 | 0.9993 |
| `wo` | 4 | 10.30 | 6.20 | 11 | 0.9993 | 6.19 | 11 | 0.9993 |
| `wo` | 8 | 10.31 | 6.19 | 22 | 0.9993 | 6.18 | 22 | 0.9993 |
| `wo` | 16 | 10.31 | 6.22 | 43 | 0.9993 | 6.21 | 43 | 0.9993 |
| `wo` | 32 | 10.28 | 8.24 | 65 | 0.9993 | 8.23 | 65 | 0.9993 |
| `wo` | 64 | 8.23 | 8.24 | 130 | 0.9993 | 8.24 | 130 | 0.9993 |
| `wo` | 128 | 8.25 | 10.26 | 209 | 0.9993 | 10.29 | 209 | 0.9993 |
| `wo` | 256 | 8.26 | 8.25 | 520 | 0.9993 | 8.26 | 520 | 0.9993 |
| `wo` | 512 | 10.30 | 8.25 | 1042 | 0.9993 | 8.26 | 1040 | 0.9993 |
| `wo` | 1024 | 14.40 | 10.29 | 1670 | 0.9993 | 10.30 | 1669 | 0.9993 |
| `wo` | 2048 | 24.61 | 14.44 | 2380 | 0.9993 | 14.44 | 2379 | 0.9993 |
| `wo` | 4096 | 43.21 | 25.21 | 2726 | 0.9993 | 24.73 | 2779 | 0.9993 |
| `wo` | 8192 | 83.44 | 47.13 | 2916 | 0.9993 | 47.14 | 2916 | 0.9993 |

Reading: both projections are at the replay floor up to M = 256 and become
bandwidth-shaped above it; `wqkv` reaches 2923 TFLOPS and `wo` 2914 TFLOPS in
MXFP8 at M = 8192. `wqkv` shows the wave-tile rule most clearly. The rule owns
its M = 128 and M = 512 cells; the first reads 6.21 µs in MXFP8, the same
three-tick floor the M = 1 cell sits at, and M = 512 reads 8.27 µs against the
10.30 µs its BF16 reference needs. Up to M = 64 both projections are on the
decode row — from this run one step further than before — and the cell that
moved with the step is `wo` at M = 64, which reads 8.24 µs in both FP8 columns
where the cascade's 2-SM wave tile read 10.27–10.30 on the previous tree, one
replay tick less; `wqkv` at M = 64 was already at its 6.20 µs floor. Two things
the older table showed remain gone: the BSFP8 column does not trail the MXFP8
one by a fraction of a timer step at `wqkv` M = 1, 16 and 32 (the scale
expansion is still paid, but the decode kernel absorbs it, and both columns
read 6.19–6.21 µs), and `wo` at M = 1–16 reads 6.18–6.21 µs where the
two-launch split-K cascade read 8.24–8.29 µs, one replay tick more.

### Grouped MoE layer (E=128, top-8, moe_inter 768) — whole layer

MoE rows on sm_100 / sm_103 are MXFP8 (1×32) on the masked grouped layout,
identical in contract to sm_120's: every expert owns a slab of `m_cap` rows,
the per-expert counts are read on device, and the kernels are chained inside
one CUDA graph. Whole-layer numbers with the same-day comparators live in
[`../layer/sm100.md`](../layer/sm100.md) (Family B), generated from
`tests/baselines/perf_moe_qwen3_30a3_b300.jsonl`.

### Grouped GEMM kernels `moe.gate_up` (N=1536, K=2048), `moe.down` (N=2048, K=768) — MXFP8

Same run and routing as the layer table; per-kernel graph-replay µs of the
grouped GEMM alone (`linear_mxfp8_grouped_masked`), TFLOPS on that kernel's
routed FLOPs, `cos` against a per-expert BF16 GEMM reference.

| op | M | m_cap | MXFP8 µs | TFLOPS | weight GB/s | cos |
|---|---:|---:|---:|---:|---:|---:|
| `moe.gate_up` | 1 | 4 | 7.9 | 6.4 | 3196 | 0.9993 |
| `moe.gate_up` | 2 | 4 | 10.3 | 9.8 | 4888 | 0.9993 |
| `moe.gate_up` | 4 | 4 | 16.4 | 12.3 | 5360 | 0.9993 |
| `moe.gate_up` | 8 | 8 | 31.0 | 13.0 | 4776 | 0.9993 |
| `moe.gate_up` | 16 | 16 | 49.1 | 16.4 | 5249 | 0.9993 |
| `moe.gate_up` | 32 | 32 | 62.0 | 26.0 | 5382 | 0.9993 |
| `moe.gate_up` | 64 | 64 | 73.7 | 43.7 | 5337 | 0.9993 |
| `moe.gate_up` | 96 | 96 | 75.1 | 64.3 | 5277 | 0.9993 |
| `moe.gate_up` | 128 | 128 | 76.0 | 84.7 | 5295 | 0.9993 |
| `moe.gate_up` | 256 | 256 | 79.0 | 163.1 | 5098 | 0.9993 |
| `moe.gate_up` | 512 | 512 | 83.8 | 307.6 | 4807 | 0.9993 |
| `moe.gate_up` | 1024 | 1024 | 85.1 | 605.9 | 4733 | 0.9993 |
| `moe.gate_up` | 2048 | 2048 | 95.2 | 1082.6 | 4229 | 0.9993 |
| `moe.gate_up` | 4096 | 4096 | 125.1 | 1648.1 | 3219 | 0.9993 |
| `moe.down` | 1 | 4 | 6.2 | 4.1 | 2033 | 0.9993 |
| `moe.down` | 2 | 4 | 8.2 | 6.1 | 3058 | 0.9993 |
| `moe.down` | 4 | 4 | 10.3 | 9.8 | 4281 | 0.9993 |
| `moe.down` | 8 | 8 | 14.4 | 14.0 | 5138 | 0.9993 |
| `moe.down` | 16 | 16 | 29.2 | 13.8 | 4419 | 0.9993 |
| `moe.down` | 32 | 32 | 35.5 | 22.7 | 4700 | 0.9993 |
| `moe.down` | 64 | 64 | 41.9 | 38.5 | 4698 | 0.9993 |
| `moe.down` | 96 | 96 | 43.2 | 55.9 | 4585 | 0.9993 |
| `moe.down` | 128 | 128 | 43.5 | 74.1 | 4628 | 0.9993 |
| `moe.down` | 256 | 256 | 44.2 | 145.8 | 4556 | 0.9993 |
| `moe.down` | 512 | 512 | 46.0 | 280.3 | 4380 | 0.9993 |
| `moe.down` | 1024 | 1024 | 50.0 | 515.5 | 4027 | 0.9993 |
| `moe.down` | 2048 | 2048 | 63.4 | 812.8 | 3175 | 0.9993 |
| `moe.down` | 4096 | 4096 | 92.3 | 1116.4 | 2181 | 0.9993 |

Reading: both projections flatten from M = 32 upward — `moe.gate_up` at
4.6–5.4 TB/s of active-weight bandwidth, `moe.down` at 3.9–4.7 TB/s — and stay
there to M = 1024, which is where the time stops growing with the token count:
`moe.gate_up` moves from 62.0 µs at M = 32 to 87.3 µs at M = 1024 while doing
32× the arithmetic. That plateau, not the 128-row tile granularity, is what
bounds the grouped path today. The evidence for that in the table is the
bandwidth column across a doubling of the row capacity: `moe.gate_up` reads
active weights at 5180 GB/s at M = 64 (`m_cap` 64) and at 5168 GB/s at M = 128
(`m_cap` 128) — the same rate, within a quarter of a per cent, for twice the
tokens and twice the slab, because the padded rows of a partially filled tile
cost no weight bytes. The direct-store epilogue changed in this run and the
table shows where. It is now partitioned so that each thread stores one row
and 64 columns whatever the N tile (an EVT store, in CUTLASS's terms), and the
cascade's rule for it was re-derived in the same session: a 192-wide tile with
five mainloop stages on short-K shapes, a 256-wide one on long-K shapes while
the expected rows per expert are at most 64. On this family `moe.gate_up`
(K = 2048) keeps the 192-wide direct store up to M = 256 and the 256-wide
TMA-store tile from M = 512, exactly the picks it had; `moe.down` (K = 768) takes the 256-wide
partitioned store from M = 128 to M = 1024, a tile the old whole-tile direct
store could not afford. Two things changed in this run's columns. At M = 64
both kernels are now on the slot-bound route, because the plain slot kernel
runs to its full 64-wide token tile (its clause stopped one row capacity short
before): `moe.gate_up` reads 73.7 µs and `moe.down` 41.9 where the previous
tree read 76.0 and 44.2 in the same session. From M = 128 up both kernels are
launched with the per-group problem shapes the routing kernel supplies and
without the argument-preparation kernel that used to precede each pointer-array
GEMM, and that is the whole of the 1.4–2.6 % the two columns move there
(`moe.gate_up` 85.1 µs at M = 1024 against 87.6, `moe.down` 50.0 against 51.1).
Above M = 2048 the weight bandwidth falls away (3219 GB/s for `moe.gate_up` at
M = 4096) because the slab is large enough that the working set no longer sits
in L2 between replays.

These are isolated kernel cells: each replays one grouped GEMM on a warm L2
with its tensor-set arrays already bound. Their decode rows are the clearest
view of the slot-bound route, which is why they move so much there. They are
not the unit a grouped route change is accepted on — that is the whole layer in
[`../layer/sm100.md`](../layer/sm100.md) ([`../README.md`](../README.md) §8).

## Family C — Qwen3.5-35B-A3B

Geometry from the model's `config.json` ([`../README.md`](../README.md) §3):
hidden 2048; 10 full-attention layers with 16 query heads × head_dim 256 and an
output gate; 30 Gated DeltaNet layers; 256 routed experts, top-8, moe_inter
512; one shared expert with intermediate 512. `gdn.in_proj` is benched as the
fused 12288-wide qkvz GEMM.

### Full-attention layer projections

#### `wqkv_gated` (N=9216, K=2048)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8.26 | 6.20 | 6 | 0.9993 | 6.19 | 6 | 0.9993 |
| 2 | 8.20 | 6.19 | 12 | 0.9993 | 6.20 | 12 | 0.9993 |
| 4 | 6.30 | 6.19 | 24 | 0.9993 | 6.20 | 24 | 0.9993 |
| 8 | 6.22 | 6.19 | 49 | 0.9993 | 6.20 | 49 | 0.9993 |
| 16 | 6.21 | 6.20 | 97 | 0.9993 | 6.20 | 97 | 0.9993 |
| 32 | 6.24 | 6.19 | 195 | 0.9993 | 6.20 | 195 | 0.9993 |
| 64 | 8.24 | 6.22 | 388 | 0.9993 | 6.20 | 390 | 0.9993 |
| 128 | 8.26 | 6.33 | 764 | 0.9993 | 6.22 | 776 | 0.9993 |
| 256 | 10.29 | 8.24 | 1173 | 0.9993 | 8.24 | 1173 | 0.9993 |
| 512 | 14.41 | 10.31 | 1875 | 0.9993 | 10.31 | 1874 | 0.9993 |
| 1024 | 24.68 | 15.51 | 2492 | 0.9993 | 14.88 | 2597 | 0.9993 |
| 2048 | 46.26 | 27.30 | 2832 | 0.9993 | 27.35 | 2826 | 0.9993 |
| 4096 | 89.06 | 53.07 | 2914 | 0.9993 | 52.93 | 2921 | 0.9993 |
| 8192 | 175.74 | 102.74 | 3010 | 0.9993 | 102.40 | 3020 | 0.9993 |

#### `wo` (N=2048, K=4096)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 10.30 | 6.18 | 3 | 0.9993 | 6.18 | 3 | 0.9993 |
| 2 | 10.31 | 6.19 | 5 | 0.9993 | 6.18 | 5 | 0.9993 |
| 4 | 10.32 | 6.19 | 11 | 0.9993 | 6.19 | 11 | 0.9993 |
| 8 | 10.31 | 6.21 | 22 | 0.9993 | 6.19 | 22 | 0.9993 |
| 16 | 10.30 | 6.23 | 43 | 0.9993 | 6.20 | 43 | 0.9993 |
| 32 | 10.23 | 8.22 | 65 | 0.9993 | 8.21 | 65 | 0.9993 |
| 64 | 8.28 | 8.25 | 130 | 0.9993 | 8.25 | 130 | 0.9993 |
| 128 | 8.25 | 10.29 | 209 | 0.9993 | 10.28 | 209 | 0.9993 |
| 256 | 8.27 | 8.26 | 520 | 0.9993 | 8.27 | 520 | 0.9993 |
| 512 | 10.30 | 8.28 | 1038 | 0.9993 | 8.27 | 1038 | 0.9993 |
| 1024 | 14.40 | 10.30 | 1668 | 0.9993 | 10.29 | 1669 | 0.9993 |
| 2048 | 24.60 | 14.45 | 2377 | 0.9993 | 14.43 | 2380 | 0.9993 |
| 4096 | 43.13 | 24.82 | 2769 | 0.9993 | 25.06 | 2742 | 0.9993 |
| 8192 | 82.50 | 47.07 | 2920 | 0.9993 | 47.07 | 2920 | 0.9993 |

### Linear-attention (Gated DeltaNet) layer projections

#### `gdn.in_proj` (N=12288, K=2048)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8.25 | 6.22 | 8 | 0.9993 | 6.19 | 8 | 0.9993 |
| 2 | 8.24 | 6.25 | 16 | 0.9993 | 6.21 | 16 | 0.9993 |
| 4 | 8.25 | 6.19 | 33 | 0.9993 | 6.21 | 32 | 0.9993 |
| 8 | 6.20 | 6.19 | 65 | 0.9993 | 6.19 | 65 | 0.9993 |
| 16 | 6.20 | 6.19 | 130 | 0.9993 | 6.18 | 130 | 0.9993 |
| 32 | 8.26 | 6.20 | 260 | 0.9993 | 6.20 | 260 | 0.9993 |
| 64 | 8.25 | 6.20 | 520 | 0.9993 | 6.23 | 517 | 0.9993 |
| 128 | 8.26 | 8.24 | 782 | 0.9993 | 7.28 | 885 | 0.9993 |
| 256 | 12.17 | 10.24 | 1258 | 0.9993 | 8.35 | 1544 | 0.9993 |
| 512 | 18.49 | 12.35 | 2086 | 0.9993 | 12.33 | 2091 | 0.9993 |
| 1024 | 33.85 | 20.55 | 2508 | 0.9993 | 20.54 | 2509 | 0.9993 |
| 2048 | 64.65 | 37.14 | 2775 | 0.9993 | 37.36 | 2759 | 0.9993 |
| 4096 | 118.68 | 70.04 | 2944 | 0.9993 | 69.74 | 2956 | 0.9993 |
| 8192 | 233.03 | 135.05 | 3053 | 0.9993 | 134.30 | 3070 | 0.9993 |

#### `gdn.out_proj` (N=2048, K=4096)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 10.32 | 6.20 | 3 | 0.9993 | 6.20 | 3 | 0.9993 |
| 2 | 10.30 | 6.20 | 5 | 0.9993 | 6.19 | 5 | 0.9993 |
| 4 | 10.28 | 6.20 | 11 | 0.9993 | 6.19 | 11 | 0.9993 |
| 8 | 10.32 | 6.20 | 22 | 0.9993 | 6.20 | 22 | 0.9993 |
| 16 | 10.31 | 6.22 | 43 | 0.9993 | 6.19 | 43 | 0.9993 |
| 32 | 10.21 | 8.24 | 65 | 0.9993 | 8.22 | 65 | 0.9993 |
| 64 | 8.29 | 8.24 | 130 | 0.9993 | 8.24 | 130 | 0.9993 |
| 128 | 8.26 | 10.27 | 209 | 0.9993 | 10.54 | 204 | 0.9993 |
| 256 | 8.27 | 8.31 | 517 | 0.9993 | 8.25 | 521 | 0.9993 |
| 512 | 10.30 | 8.28 | 1037 | 0.9993 | 8.24 | 1042 | 0.9993 |
| 1024 | 14.41 | 10.31 | 1666 | 0.9993 | 10.30 | 1668 | 0.9993 |
| 2048 | 24.59 | 14.39 | 2388 | 0.9993 | 14.42 | 2382 | 0.9993 |
| 4096 | 43.12 | 25.11 | 2736 | 0.9993 | 25.00 | 2748 | 0.9993 |
| 8192 | 83.45 | 47.13 | 2916 | 0.9993 | 47.15 | 2915 | 0.9993 |

### Grouped MoE layer (E=256, top-8, moe_inter 512) — whole block

Whole-block numbers (routed layer, routed + shared expert, and the same-day
comparators) live in [`../layer/sm100.md`](../layer/sm100.md) (Family C),
generated from `tests/baselines/perf_moe_qwen3_35a3_b300.jsonl` and
`tests/baselines/perf_moe_qwen3_35a3_shared_b300.jsonl`.

### Grouped GEMM kernels `moe.gate_up` (N=1024, K=2048), `moe.down` (N=2048, K=512) — MXFP8

| op | M | m_cap | MXFP8 µs | TFLOPS | weight GB/s | cos |
|---|---:|---:|---:|---:|---:|---:|
| `moe.gate_up` | 1 | 4 | 6.2 | 5.4 | 2698 | 0.9993 |
| `moe.gate_up` | 2 | 4 | 8.2 | 8.1 | 4069 | 0.9993 |
| `moe.gate_up` | 4 | 4 | 10.3 | 13.0 | 6103 | 0.9993 |
| `moe.gate_up` | 8 | 8 | 24.6 | 10.9 | 4941 | 0.9993 |
| `moe.gate_up` | 16 | 16 | 41.4 | 13.0 | 5070 | 0.9993 |
| `moe.gate_up` | 32 | 32 | 63.8 | 16.8 | 5289 | 0.9993 |
| `moe.gate_up` | 64 | 64 | 87.8 | 24.5 | 5278 | 0.9993 |
| `moe.gate_up` | 96 | 96 | 94.6 | 34.1 | 5365 | 0.9993 |
| `moe.gate_up` | 128 | 128 | 98.2 | 43.7 | 5380 | 0.9993 |
| `moe.gate_up` | 256 | 256 | 100.4 | 85.6 | 5349 | 0.9993 |
| `moe.gate_up` | 512 | 512 | 102.3 | 167.9 | 5247 | 0.9993 |
| `moe.gate_up` | 1024 | 1024 | 105.5 | 325.7 | 5089 | 0.9993 |
| `moe.gate_up` | 2048 | 2048 | 110.8 | 620.2 | 4845 | 0.9993 |
| `moe.gate_up` | 4096 | 4096 | 126.3 | 1088.0 | 4250 | 0.9993 |
| `moe.down` | 1 | 4 | 6.2 | 2.7 | 1357 | 0.9993 |
| `moe.down` | 2 | 4 | 6.3 | 5.3 | 2644 | 0.9993 |
| `moe.down` | 4 | 4 | 10.3 | 6.5 | 3068 | 0.9993 |
| `moe.down` | 8 | 8 | 12.5 | 10.7 | 4861 | 0.9993 |
| `moe.down` | 16 | 16 | 24.6 | 10.9 | 4257 | 0.9993 |
| `moe.down` | 32 | 32 | 38.0 | 14.1 | 4438 | 0.9993 |
| `moe.down` | 64 | 64 | 48.3 | 22.2 | 4800 | 0.9993 |
| `moe.down` | 96 | 96 | 51.7 | 31.2 | 4911 | 0.9993 |
| `moe.down` | 128 | 128 | 54.3 | 39.6 | 4869 | 0.9993 |
| `moe.down` | 256 | 256 | 56.0 | 76.7 | 4796 | 0.9993 |
| `moe.down` | 512 | 512 | 57.2 | 150.2 | 4693 | 0.9993 |
| `moe.down` | 1024 | 1024 | 60.4 | 284.4 | 4443 | 0.9993 |
| `moe.down` | 2048 | 2048 | 68.1 | 504.7 | 3943 | 0.9993 |
| `moe.down` | 4096 | 4096 | 98.5 | 697.4 | 2724 | 0.9993 |

### Shared expert (dense, every token)

#### `shared.gate_up` (N=1024, K=2048)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8.27 | 6.19 | 1 | 0.9993 | 6.18 | 1 | 0.9993 |
| 2 | 8.24 | 6.18 | 1 | 0.9993 | 6.16 | 1 | 0.9993 |
| 4 | 8.24 | 6.19 | 3 | 0.9993 | 6.18 | 3 | 0.9993 |
| 8 | 8.23 | 6.19 | 5 | 0.9993 | 6.18 | 5 | 0.9993 |
| 16 | 8.25 | 6.20 | 11 | 0.9993 | 6.20 | 11 | 0.9993 |
| 32 | 6.21 | 6.19 | 22 | 0.9993 | 6.19 | 22 | 0.9993 |
| 64 | 6.19 | 6.21 | 43 | 0.9993 | 6.20 | 43 | 0.9993 |
| 128 | 6.19 | 6.22 | 86 | 0.9993 | 6.21 | 86 | 0.9993 |
| 256 | 6.20 | 6.22 | 173 | 0.9993 | 6.22 | 173 | 0.9993 |
| 512 | 6.21 | 6.22 | 345 | 0.9993 | 6.19 | 347 | 0.9993 |
| 1024 | 8.24 | 6.22 | 691 | 0.9993 | 6.21 | 692 | 0.9993 |
| 2048 | 8.44 | 6.24 | 1377 | 0.9993 | 6.31 | 1362 | 0.9993 |
| 4096 | 14.41 | 10.30 | 1668 | 0.9993 | 10.28 | 1671 | 0.9993 |
| 8192 | 24.62 | 14.46 | 2376 | 0.9993 | 14.50 | 2369 | 0.9993 |

#### `shared.down` (N=2048, K=512)

| M | BF16 µs | BSFP8 µs | BSFP8 TFLOPS | BSFP8 cos | MXFP8 µs | MXFP8 TFLOPS | MXFP8 cos |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 4.18 | 4.17 | 1 | 0.9992 | 4.16 | 1 | 0.9992 |
| 2 | 4.17 | 4.18 | 1 | 0.9993 | 4.17 | 1 | 0.9993 |
| 4 | 4.17 | 4.17 | 2 | 0.9993 | 4.16 | 2 | 0.9993 |
| 8 | 4.15 | 4.15 | 4 | 0.9993 | 4.15 | 4 | 0.9993 |
| 16 | 4.21 | 4.15 | 8 | 0.9993 | 4.15 | 8 | 0.9993 |
| 32 | 4.16 | 4.14 | 16 | 0.9993 | 4.17 | 16 | 0.9993 |
| 64 | 4.16 | 5.27 | 25 | 0.9993 | 4.24 | 32 | 0.9993 |
| 128 | 4.15 | 6.14 | 44 | 0.9993 | 4.15 | 65 | 0.9993 |
| 256 | 4.15 | 4.97 | 108 | 0.9993 | 4.17 | 129 | 0.9993 |
| 512 | 4.18 | 6.17 | 174 | 0.9993 | 4.16 | 258 | 0.9993 |
| 1024 | 6.20 | 6.17 | 348 | 0.9993 | 4.24 | 506 | 0.9993 |
| 2048 | 8.22 | 6.21 | 692 | 0.9993 | 6.21 | 692 | 0.9993 |
| 4096 | 10.31 | 8.26 | 1040 | 0.9993 | 8.24 | 1043 | 0.9993 |
| 8192 | 14.41 | 12.34 | 1392 | 0.9993 | 12.34 | 1392 | 0.9993 |

Reading (Family C): the four dense projections behave like Family B's — at the
replay floor to M = 128–256, then bandwidth-shaped, topping out at 3023 TFLOPS
(`wqkv_gated`) and 3038 TFLOPS (`gdn_in_proj`) in MXFP8 at M = 8192. From this
run the decode row serves M = 33–64 on every projection of this family but
`shared_down` (K = 512 is below the row's 1024-deep K floor), and the cells that
move with it are the ones the cascade's wave tile or split-K served before: `wo`
and `gdn_out_proj` read 8.25 µs at M = 64 in both FP8 columns where the previous
tree read 10.28–10.29, and `gdn_in_proj` reads 6.21–6.23 where it read
8.23–8.25, one replay tick less each; `wqkv_gated` and `shared_gate_up` were
already at 6.20–6.23 there. The wave-tile rule owns `wqkv_gated` at M = 128,
which is why both FP8 columns read 6.22–6.33 µs there while the BF16 reference
needs 8.26 µs, and `gdn_in_proj` at M = 128 and M = 256, where MXFP8 reads 7.28
and 8.56 µs against a BF16 reference of 8.26 and 12.29. Those two `gdn_in_proj`
cells are also the ones this device scatters on: the 2026-09-17 run recorded
them as scatter rather than as a property of the tree, the 2026-09-22 run's
nsys showed the identical cuBLAS kernel on both trees, and in this run they
again read outside the ±1 % band against the same-session record while six
passes per tree put the medians at 6.82 against 6.59 µs (M = 128) and 8.51
against 8.67 (M = 256) with each tree scattering 8–10 % on its own, so read them
as unstable cells rather than as a movement. The wave-tile rule does not fire
on `wo`, `gdn_out_proj` or either shared-expert shape: their N is narrow enough
that the narrow tile would be handed a grid of 32 or 64 CTAs, far short of the
half-machine bound the rule requires, so above the decode row they keep their
previous route. The two shared-expert shapes are small enough that they never
leave the floor over most of the grid: `shared_down` (N=2048, K=512) reads
4.14–4.24 µs from M = 1 to M = 1024 in MXFP8, so the shared expert costs a fixed
launch-shaped amount per layer until the batch is large. Its BSFP8 column is
the one place where the scale-expansion step is visible as more than a timer
step (5.3–6.2 µs against 4.2 µs at M = 64–1024), because the GEMM itself is
only four microseconds long. The grouped kernels reach a higher plateau than
Family B's — 5.1–5.4 TB/s on `moe.gate_up` from M = 64 to M = 512 — since 256
experts of 512-wide intermediate give the scheduler more independent groups to
keep in flight. The direct-store rule fires here on `moe.down` only: with
K = 512 it is a short-K shape and takes the 192-wide partitioned store with
five mainloop stages at every M from 64 up — N = 2048 is not a multiple of 192,
which the partitioned store no longer needs; `moe.gate_up` (N = 1024, K = 2048)
keeps the 256-wide TMA-store tile it had at every M. Both columns move in this
run for the same reason as Family B's: from M = 128 (`moe.gate_up`) and M = 64
(`moe.down`) the kernels launch with routing-supplied problem shapes and no
argument-preparation kernel ahead of them (`moe.gate_up` 105.5 µs at M = 1024
against 107.3 in the same session, `moe.down` 60.4 against 61.1), and at
M = 32–64 `moe.down`'s plain slot kernel now runs to its full tile (48.4 µs at
M = 64 against 50.2).
